{
  "name": "rest",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "private": true,
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.7.0",
    "rimraf": "^5.0.1",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "rimraf dist && openapi-generator-cli generate && npx tsc"
  }
}
